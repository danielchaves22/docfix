# API Service

Scaffold inicial para API Node/TypeScript.

## Próximos passos sugeridos
- Criar `src/` com bootstrap HTTP.
- Configurar migrations em `services/api/migrations` com `node-pg-migrate`.
- Definir contrato OpenAPI em `packages/contracts`.
